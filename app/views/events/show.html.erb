<h1><%= @event.name %> <%= @event.service %></h1>

<footer>
  <%= link_to "Edit", edit_schedule_event_path(@schedule, @event) %>
  <%= link_to "Delete", schedule_event_path(@schedule, @event), method: :delete, data: {confirm: "Are you sure?"}  %>
  <p>
  <%= link_to "All Schedules", schedules_path %>
  </p>
</footer>
